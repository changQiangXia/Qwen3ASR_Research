# =============================================================================
# 安装说明（必读）：
# 1. 先执行 Step 1-2 安装 PyTorch GPU 版本（不要跳过！）
# 2. 然后再执行 Step 3 安装本文件中的剩余依赖
# =============================================================================

# Step 2 中已安装的依赖（此处注释掉，防止覆盖安装）
# torch>=2.0.0
# torchaudio>=2.0.0

# ------------------------------------------------------------------------------
# 核心依赖（无需 GPU）
# ------------------------------------------------------------------------------
transformers>=4.40.0
accelerate>=0.25.0

# 量化相关
bitsandbytes>=0.43.0
optimum>=1.18.0

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0  # 用于 m4a/mp3 等格式转换

# 数据与可视化
numpy>=1.24.0,<2.0.0  # 限制 numpy 版本，避免与某些库不兼容
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=2.0.0

# 模型下载（解决 HuggingFace 慢的问题）
modelscope>=1.15.0
huggingface-hub>=0.20.0

# 数据集与评估
datasets>=2.14.0
jiwer>=3.0.0

# Web UI
gradio>=4.0.0

# 工具
tqdm>=4.65.0
tensorboard>=2.14.0
